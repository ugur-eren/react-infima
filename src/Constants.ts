import {Size, Theme} from './Types';

export const Themes: Theme[] = ['primary', 'secondary', 'success', 'info', 'warning', 'danger'];

export const DefaultTheme: Theme = 'secondary';

export const DefaultSize: Size = 'normal';
